#include<stdio.h>
#include<math.h>
#define PI 3
//cone
int main() {
int h,r,v;

printf("enter h:");
scanf("%d",&h);

printf("enter r:");
scanf("%d",&r);

v = ((PI*r*r)*h)/3;
printf("v = %d",v);

    return 0;
}
